# Dependencies
node_modules/

# Environment (never commit secrets)
.env
.env.local
.env.*.local
!.env*.example
!**/.env.production.example
!**/.env.development.example

# Builds
dist/
build/

# Logs
*.log
npm-debug.log*
pm2-*.log

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Symlink dist folders (no spaces — used by Nginx)
frontend-dist
leads-dist


# Uploads cache (keep server uploads out of git if large)
# Backend (5)/Backend/uploads/
